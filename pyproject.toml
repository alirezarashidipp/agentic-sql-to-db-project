[project]
name = "sql-to-db-project"
version = "0.1.0"
description = "Ask employee questions and answer them through OpenAI and SQLite"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.116",
    "langgraph>=1.0",
    "openai>=2",
    "pyyaml>=6.0",
    "uvicorn>=0.35",
]
